# 🚨 EMERGENCY: Telegram агент не отправляет реактивацию

## ЧТО ДЕЛАТЬ ПРЯМО СЕЙЧАС

### Шаг 1: ПЕРЕСОБЕРИТЕ проект

```bash
cd /Users/worker/Documents/GitHub/openclaw
pnpm build
```

**Дождитесь завершения!** (займет ~30 секунд)

### Шаг 2: НАЙДИТЕ и УБЕЙТЕ gateway процесс

```bash
# Найти процесс
ps aux | grep openclaw | grep gateway

# Убить (замените <PID> на номер процесса)
kill -9 <PID>

# ИЛИ убить все сразу:
pkill -9 -f "openclaw.*gateway"
```

### Шаг 3: ЗАПУСТИТЕ gateway заново

```bash
cd /Users/worker/Documents/GitHub/openclaw
nohup node dist/index.js gateway run --force &

# ИЛИ если у вас установлен openclaw глобально:
nohup openclaw gateway run --force &
```

### Шаг 4: ПОДОЖДИТЕ 60 секунд

Gateway должен:
1. Запустить cron реактивации
2. Выполнить первую проверку сразу при старте
3. Отправить сообщения подходящим контактам

### Шаг 5: ПРОВЕРЬТЕ логи

```bash
tail -f nohup.out | grep "re-engagement"
```

**Ищите:**
```
✅ [TG:AgentName] re-engagement check | delays=[...] trackedContacts=3
✅ [TG:AgentName] re-engagement day=3 window=[...] duration=72.0h found=2
✅ [TG:AgentName] re-engagement sent | chat=... day=3
```

---

## ЕСЛИ ВСЕ РАВНО НЕ РАБОТАЕТ

### Проблема 1: Настройки не сохранены

**Проверьте через UI:**
1. Откройте вкладку "Промпты"
2. Прокрутите до "🔁 Реактивация"
3. Убедитесь:
   - ✅ **Реактивация ВКЛЮЧЕНА**
   - ✅ **Шаблон заполнен** (например: "Привет {имя}!")
   - ✅ **Интервал правильный** (например: от 2 до 4 дней)

**После изменения:**
- Кликните мышкой ВНЕ поля ввода
- Подождите 1 секунду
- Рестартуйте агента (Stop → Start в UI)

### Проблема 2: Интервал не подходит

**Ваши контакты молчат 2.86 - 3.73 дня**

Установите интервал **2-4 дня**:
```
Интервал молчания: от 2 до 4 дней
☑ и более
```

НЕ используйте 1 день - контакты не попадут!

### Проблема 3: Нет шаблона или AI режим без истории

**Если режим "📝 Шаблон":**
- Шаблон ОБЯЗАТЕЛЕН!
- Пример: "Привет {имя}! Как дела?"

**Если режим "🤖 ИИ генерирует":**
- Шаблон НЕ нужен
- НО должна быть история чата

### Проблема 4: AI выключен

Проверьте вкладку "Обзор":
- **AI-ответы:** должно быть ВКЛ (зеленый переключатель)

Если AI выключен → реактивация не работает!

---

## БЫСТРЫЙ ТЕСТ

Хотите проверить что реактивация ВООБЩЕ работает?

### 1. Установите тестовые настройки

Вкладка "Промпты" → "Реактивация":
```
✅ Включить
Интервал: от 1 до 1 день
Шаблон: "ТЕСТ {имя}"
Режим: 📝 Шаблон
```

### 2. Отправьте тестовое сообщение

От любого аккаунта отправьте сообщение боту:
```
Привет!
```

### 3. Подождите 25 часов

Да, нужно реально ждать 1 день + запас.

### 4. Проверьте

Через 25 часов рестартуйте агента и проверьте логи.

Должно прийти сообщение "ТЕСТ ИмяКонтакта".

---

## SQL ДИАГНОСТИКА (если есть доступ к БД)

```bash
sqlite3 ~/.openclaw/gateway.db

# Проверить агентов
SELECT name, status FROM telegram_agents;

# Проверить контакты
SELECT 
  chat_id,
  first_name,
  ROUND((julianday('now') - julianday(last_client_msg_at)), 2) as silence_days
FROM tg_contacts
ORDER BY last_client_msg_at DESC
LIMIT 10;

# Проверить настройки
SELECT 
  json_extract(settings, '$.reEngagementEnabled') as enabled,
  json_extract(settings, '$.reEngagementDelayFrom') as delayFrom,
  json_extract(settings, '$.reEngagementDelayTo') as delayTo
FROM telegram_agents;

# Проверить отправки
SELECT * FROM tg_reengagement ORDER BY sent_at DESC LIMIT 10;
```

---

## КРИТИЧЕСКИЕ ПРОВЕРКИ

- [ ] Проект пересобран (`pnpm build` выполнен)
- [ ] Gateway перезапущен (kill + start)
- [ ] AI включен в UI (вкладка Обзор)
- [ ] Реактивация включена (вкладка Промпты)
- [ ] Шаблон заполнен (если режим "Шаблон")
- [ ] Интервал соответствует времени молчания (2-4 дня для ваших контактов)
- [ ] Подождали 60 секунд после рестарта
- [ ] Проверили логи (`tail -f nohup.out | grep re-engagement`)

---

## ЕСЛИ НИЧЕГО НЕ ПОМОГЛО

Отправьте ПОЛНЫЕ логи:

```bash
tail -500 nohup.out | grep -A 5 -B 5 "re-engagement"
```

И скриншот настроек из вкладки "Промпты" → "Реактивация".

**Возможные причины:**
1. Код не пересобрался (старая версия)
2. Настройки не сохранились в БД
3. Cron не запустился
4. Контакты не попадают в окно (неправильный интервал)
5. Нет шаблона в режиме "Шаблон"

Но с расширенным окном ±36 часов и правильным интервалом 2-4 дня - **должно работать!**

